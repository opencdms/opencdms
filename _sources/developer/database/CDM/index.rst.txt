Climate Data Model
==================

The data model has been documented via the API definition below. This incorporates elements of the WMDR data model,
the WCMP2 data model and the early draft climate data model.

.. raw:: html

    <style>

      /* Hide the sphinx RHS toc that has no contents on this page */
      .bd-sidebar-secondary { display: none; }

      /* Hide the redoc LHS menu */
      .menu-content { display: none; }

      /* Disable RHS bg grey color bar (not displaying correctly) */
      .bCOLCR { display: none; }

      /* Use all of the available width */
      .api-content { width: 100%; }

      /* Ensure styles below are uncommented to stack panels vertically
         (column) instead of side-by-side and use full width */

      .hutltu { width: 100%; }

      .dolNKP {
        display: flex;
        width: 100%;
        padding: 0px;
        flex-direction: column;
      }

      .dYGhuI {
        width: 100%;
        padding: 40px;
        margin-top: 40px;
      }
    </style>
    <redoc spec-url='../../../_static/openapi/datamodel.yml'>
    </redoc>
    <script src="https://cdn.jsdelivr.net/npm/redoc@next/bundles/redoc.standalone.js"> </script>

